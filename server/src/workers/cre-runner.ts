// Stub para evitar import circular — lógica CRE real em brain_cre.tools.ts
export async function runCreCycle(_projectId: string, _data: unknown, _openai: unknown, _job: unknown) {
  return { message: "CRE cycle via queue — em desenvolvimento" };
}
